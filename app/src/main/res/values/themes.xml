<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NGXMobileClient" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_99</item>
        <item name="colorPrimaryVariant">@color/primary_40</item>
        <item name="colorOnPrimary">@color/primary_80</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <!-- Customize your theme here. -->


    </style>

    <style name="Theme.NGXMobileClient.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NGXMobileClient.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/primary_40</item>
    </style>

    <style name="Theme.NGXMobileClient.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LoginEditText" parent="ThemeOverlay.Material3.TextInputEditText">
        <item name="colorControlNormal">@android:color/darker_gray</item>
        <item name="colorControlActivated">@color/black</item>
    </style>
</resources>